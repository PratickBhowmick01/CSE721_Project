
	# result = [[sum(a * b for a, b in zip(arr_row, matrix_col))
	# 												for matrix_col in zip(*matrix)]
	# 													for arr_row in arr]
	# for row in result:
	# 	for elem in row:
	# 		elem = (elem % 26) + 97
	# 		cipher += chr(elem)

	# return cipher.upper()